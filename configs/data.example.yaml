# Ultralytics data yaml example for stage-1 Detect training.
# Copy to $COUNT_DATA/yolo/pest.yaml and edit paths.

path: ./data/yolo
train: images/train
val: images/val
test: images/test

names:
  0: pest

nc: 1
